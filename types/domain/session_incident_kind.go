// Code generated by tapir. DO NOT EDIT.

package domain

// Why a session-backed whatsapp account (native/stevo) last stopped working.
//
// [`crate::channel::Channel`]-agnostic, and split by what an operator has to
// *do* about it — which is why the three terminal cases are separate variants
// rather than one "logged out". The platform reports them as three different
// codes and they call for three different responses: re-pair, wait, or give up
// on the number. Collapsing them would throw away the one thing that tells
// them apart.
type SessionIncidentKind string

const (
	SessionIncidentKindTemporaryBan SessionIncidentKind = "temporary_ban"
	SessionIncidentKindBanned SessionIncidentKind = "banned"
	SessionIncidentKindLoggedOut SessionIncidentKind = "logged_out"
	SessionIncidentKindMainDeviceGone SessionIncidentKind = "main_device_gone"
	SessionIncidentKindClientOutdated SessionIncidentKind = "client_outdated"
	SessionIncidentKindStreamReplaced SessionIncidentKind = "stream_replaced"
	SessionIncidentKindConnectFailure SessionIncidentKind = "connect_failure"
	SessionIncidentKindStreamError SessionIncidentKind = "stream_error"
	SessionIncidentKindReachoutTimelock SessionIncidentKind = "reachout_timelock"
	SessionIncidentKindRateLimited SessionIncidentKind = "rate_limited"
)
