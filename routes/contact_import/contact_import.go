// Code generated by tapi. DO NOT EDIT.

package contact_import

import (
	__client "integro_sdk"
	__strings "strings"
	contact_import "integro_sdk/types/contact_import"
	domain "integro_sdk/types/domain"
)

// Cancel Cancel the import: every still-queued row becomes `skipped`. Terminal;
// conversations already created stay.
//
// Requires `ImportContacts` in the import's group.
func Cancel(__c *__client.Client, importUid string) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import/{import_uid}/cancel"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[contact_import.ContactImportDetail](__c, "POST", __path, nil, nil)
}
// Delete Delete an import and its rows. Refused while loading — pause or cancel
// first. Conversations already created stay.
//
// Requires `ImportContacts` in the import's group.
func Delete(__c *__client.Client, importUid string) (struct{}, error) {
	__path := "/contact-import/{import_uid}"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[struct{}](__c, "DELETE", __path, nil, nil)
}
// DeleteRow Drop a staged row instead of fixing it: the record simply leaves the
// import.
//
// Requires `ImportContacts` in the import's group.
func DeleteRow(__c *__client.Client, importUid string, rowId string) (struct{}, error) {
	__path := "/contact-import/{import_uid}/row/{row_id}"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	__path = __strings.Replace(__path, "{row_id}", __client.EncodePath(rowId), 1)
	return __client.Request[struct{}](__c, "DELETE", __path, nil, nil)
}
// Get One import with its derived counters and drain estimate — the progress
// page's poll.
//
// Requires `ImportContacts` in the import's group.
func Get(__c *__client.Client, importUid string) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import/{import_uid}"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[contact_import.ContactImportDetail](__c, "GET", __path, nil, nil)
}
// List List imports, newest first, optionally one group's.
//
// Requires `ImportContacts`; the list covers only imports of groups where the caller holds it.
func List(__c *__client.Client, __query contact_import.ListImportsQuery) ([]contact_import.ContactImportDetail, error) {
	__path := "/contact-import"
	return __client.Request[[]contact_import.ContactImportDetail](__c, "GET", __path, __query, nil)
}
// Original Download the file as it was parsed: the original columns and cells, one
// line per record still in the import — rows deleted while staged are not
// in it. Cells are re-quoted per RFC 4180, so byte-for-byte it may differ
// from the upload.
//
// Requires `ImportContacts` in the import's group.
func Original(__c *__client.Client, importUid string) ([]byte, error) {
	__path := "/contact-import/{import_uid}/original.csv"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.RequestBytes(__c, "GET", __path, nil, nil)
}
// Pause Pause loading: queued rows stay queued and the loaders skip them until a
// resume. Rows already being checked finish.
//
// Requires `ImportContacts` in the import's group.
func Pause(__c *__client.Client, importUid string) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import/{import_uid}/pause"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[contact_import.ContactImportDetail](__c, "POST", __path, nil, nil)
}
// Report Download the import's report: the original columns plus the normalized
// phone, the verdict and its detail, one line per record.
//
// Requires `ImportContacts` in the import's group.
func Report(__c *__client.Client, importUid string) ([]byte, error) {
	__path := "/contact-import/{import_uid}/report.csv"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.RequestBytes(__c, "GET", __path, nil, nil)
}
// Resume Resume a paused import: the loaders pick the queue back up where it stood.
//
// Requires `ImportContacts` in the import's group.
func Resume(__c *__client.Client, importUid string) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import/{import_uid}/resume"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[contact_import.ContactImportDetail](__c, "POST", __path, nil, nil)
}
// Rows A page of the import's rows in file order, optionally one verdict only —
// the fix table and the verdict drill-downs.
//
// Requires `ImportContacts` in the import's group.
func Rows(__c *__client.Client, importUid string, __query contact_import.ListImportRowsQuery) ([]domain.ContactImportRow, error) {
	__path := "/contact-import/{import_uid}/row"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[[]domain.ContactImportRow](__c, "GET", __path, __query, nil)
}
// Start Start loading: pending rows are dealt round-robin across the accounts and
// one paced loader per account begins verifying numbers and creating
// conversations. Rows are no longer editable.
//
// Requires `ImportContacts` in the import's group.
func Start(__c *__client.Client, importUid string) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import/{import_uid}/start"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[contact_import.ContactImportDetail](__c, "POST", __path, nil, nil)
}
// Update Rename the import. Any status.
//
// Requires `ImportContacts` in the import's group.
func Update(__c *__client.Client, importUid string, __body contact_import.UpdateContactImportRequest) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import/{import_uid}"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	return __client.Request[contact_import.ContactImportDetail](__c, "PUT", __path, nil, __body)
}
// UpdateRow Fix a staged row: the corrected values are re-parsed and the row gets its
// new verdict. Only parse-phase verdicts are editable, and only while the
// import is staged.
//
// Requires `ImportContacts` in the import's group.
func UpdateRow(__c *__client.Client, importUid string, rowId string, __body contact_import.UpdateImportRowRequest) (domain.ContactImportRow, error) {
	__path := "/contact-import/{import_uid}/row/{row_id}"
	__path = __strings.Replace(__path, "{import_uid}", __client.EncodePath(importUid), 1)
	__path = __strings.Replace(__path, "{row_id}", __client.EncodePath(rowId), 1)
	return __client.Request[domain.ContactImportRow](__c, "PUT", __path, nil, __body)
}
// Upload Upload a CSV of contacts: the file is parsed synchronously, every record
// gets a parse verdict, and the import is created `staged` — nothing touches
// a platform until `start`. Invalid rows stay editable while staged.
//
// Requires `ImportContacts` in the accounts' group; every account must be a whatsapp session account (stevo or native) of that same group.
func Upload(__c *__client.Client, __form *__client.MultipartForm) (contact_import.ContactImportDetail, error) {
	__path := "/contact-import"
	return __client.RequestMultipart[contact_import.ContactImportDetail](__c, "POST", __path, nil, __form)
}
